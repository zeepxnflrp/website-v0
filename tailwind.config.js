module.exports = {
    content: [
        "./src/**/*.{js,jsx,ts,tsx}", // Adjust this path as needed for your project
    ],
    theme: {
        extend: {},
    },
    plugins: [require("daisyui")],
}